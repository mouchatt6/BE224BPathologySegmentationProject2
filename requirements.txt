# BE224B Project 2 — Prostate Pathology Patch Classification
# Pinned to the versions verified working on Apple Silicon (M3 Pro, MPS) / Python 3.12.
#
# Install:  python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# NOTE: Macenko stain normalization is implemented in pure NumPy (src/data/stain.py) —
#       no third-party stain library is required.

torch==2.12.0
torchvision==0.27.0
timm==1.0.27
transformers==5.10.2          # Path B: owkin/phikon foundation model
albumentations==2.0.8
opencv-python-headless==4.13.0.92
scikit-learn==1.9.0
scipy==1.17.1
pandas==3.0.3
numpy==2.4.6
PyYAML==6.0.3
tqdm==4.68.1
Pillow==12.2.0

# ---- testing ----
pytest==9.0.3
