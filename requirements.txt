# BE224B Project 2 — Prostate Pathology Patch Classification
# Path A (frozen CNN features + MLP head) dependencies.
# Pinned to the versions verified working on Apple Silicon (M3 Pro, MPS) / Python 3.12.
#
# Install:  python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# NOTE: Path B (Day 3) adds `transformers` for owkin/phikon; the Day-4 improvement
#       layer adds `torchstain` for Macenko stain normalization. They are intentionally
#       omitted here to keep the install footprint small and are added when those days arrive.

torch==2.12.0
torchvision==0.27.0
timm==1.0.27
albumentations==2.0.8
opencv-python-headless==4.13.0.92
scikit-learn==1.9.0
scipy==1.17.1
pandas==3.0.3
numpy==2.4.6
PyYAML==6.0.3
tqdm==4.68.1
Pillow==12.2.0
